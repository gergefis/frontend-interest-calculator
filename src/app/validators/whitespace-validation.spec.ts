import { WhitespaceValidation } from './whitespace-validation';

describe('WhitespaceValidation', () => {
  it('should create an instance', () => {
    expect(new WhitespaceValidation()).toBeTruthy();
  });
});
